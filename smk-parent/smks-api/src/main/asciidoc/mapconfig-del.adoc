== {DELETE} MapConfiguration - Delete a Map Configuration

Executing a DELETE at the MapConfiguration endpoint will permenantly delete an existing Map Configuration, including all attachments

WARNING: This operation cannot be undone.

Example of curl command:

include::{snippets}/mapconfig-del/curl-request.adoc[]

Path Parameters:

include::{snippets}/mapconfig-del/path-parameters.adoc[]

Example of http request:

include::{snippets}/mapconfig-del/http-request.adoc[]

Example of http response:

include::{snippets}/mapconfig-del/http-response.adoc[]

Response body:

include::{snippets}/mapconfig-del/response-body.adoc[]